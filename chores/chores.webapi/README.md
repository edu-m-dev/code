This project is a minimal Web API for chores. It uses MediatR and NSwag for OpenAPI.

To run:

- Ensure connection string in appsettings.json
- dotnet run --project chores/chores.webapi

To add migrations, use the chores.migrations project.